[project]
name = "rename-py"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "libcst>=1.8.2",
    "pytest>=8.4.1",
    "typer>=0.16.1",
]

[project.scripts]
rename-py = "rename_py.cli:app"
